on: push
jobs:
  test:
    runs-on:
    - self-hosted
    - freebsd
    steps:
    - run: echo Hello World
    - run: whoami
    - run: node --version
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '17'
    - run: node --version
